import React from 'react';

const PredictorImg = ({ className }) => {
  return (
    <img src="/predictor.svg" alt="Predictor Img" className={` ${className}`} />
  );
};

export default PredictorImg;
