import React from 'react';
import { IoPrintOutline } from 'react-icons/io5';

const PrintIcon = ({ className = ' ' }) => {
  return <IoPrintOutline className={className} />;
};

export default PrintIcon;
